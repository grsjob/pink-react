import React from 'react';
import PropTypes from 'prop-types';
import {StylesSectionFooter} from "./styles";

const Footer = props => {
    return (
        <StylesSectionFooter>
            <h2>
                footer
            </h2>
        </StylesSectionFooter>

    );
};

Footer.propTypes = {};

export default Footer;
