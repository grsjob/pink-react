import React from 'react';
import PropTypes from 'prop-types';

const MainPage = props => {
    return (
        <h1>
            main-page.jsx
        </h1>
    );
};

MainPage.propTypes = {
    
};

export default MainPage;
