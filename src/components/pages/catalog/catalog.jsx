import React from 'react';
import PropTypes from 'prop-types';

const Catalog = props => {
    return (
        <h1>
            catalog
        </h1>
    )
};

Catalog.propTypes = {
    
};

export default Catalog;
